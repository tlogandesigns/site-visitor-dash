# Zapier Webhook Configuration
# Create a Zap with a "Webhooks by Zapier" trigger (Catch Hook)
# Copy the webhook URL and paste it here
# Example: https://hooks.zapier.com/hooks/catch/123456/abcdef/
ZAPIER_WEBHOOK_URL=your_zapier_webhook_url_here

# Zapier Webhook for 60-day represented visitor follow-up emails
# Create a separate Zap (Catch Hook trigger -> Email action) that reads
# "recipientEmail" from the payload and sends the follow-up email
ZAPIER_REPRESENTED_FOLLOWUP_WEBHOOK_URL=your_zapier_webhook_url_here

# JWT Authentication
JWT_SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Database
DATABASE_PATH=/data/leads.db

# Super Admin Configuration
# This user will be automatically created/updated on application startup
SUPER_ADMIN_USERNAME=superadmin
SUPER_ADMIN_PASSWORD=ChangeMeOnFirstLogin123!
SUPER_ADMIN_EMAIL=admin@example.com

# Google Sheets Import (Optional)
GOOGLE_SHEET_KEY=
WORKSHEET_NAME=Agents
